import mocha from "mocha"
import { assert } from "chai"

describe('placeholder', function() {
  it('should assert true', () => {
    assert.isTrue(true)
  } )
  it('should asser false', () => {
    assert.isFalse(false)
  } )
});
